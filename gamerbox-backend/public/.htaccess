<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si el archivo o directorio físico existe, no reescribe
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Redirige todo a index.php
    RewriteRule ^ index.php [QSA,L]
</IfModule>
